# docs

`REPRODUCIBILITY.md` — audit of the dependency graph, the five points where the original code was
not kept, and how four of them were rebuilt and checked against the published figures.

`DEPENDENCY_GRAPH.md` — the minimal chain, which notebook produces each intermediate, orphans, and
the superseded branches that were excluded.

`PATHS.md` — the hard-coded absolute path to edit in each notebook, and the directory layout they
expect.

The thesis text is not included while it is unpublished.
